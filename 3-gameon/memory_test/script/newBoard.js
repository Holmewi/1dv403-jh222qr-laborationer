"use strict";

function MemoryBoard(flippedTiles, randomNumbers){
    flippedTiles = 0;
    var output = "";
    var rows = 4;
    var cols = 4;
    randomNumbers = RandomGenerator.getPictureArray(rows, cols);
};